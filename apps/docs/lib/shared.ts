// bimorph is a placeholder project name (see README) — kept consistent here so a
// later rename is a single-file change.
export const appName = 'bimorph';

// Canonical production origin — the single source of truth for absolute URLs
// (metadataBase, sitemap, Open Graph images, llms.txt examples).
export const siteUrl = 'https://bimorph.dev';

export const docsRoute = '/docs';
export const docsImageRoute = '/og/docs';
export const docsContentRoute = '/llms.mdx/docs';

// GitHub repository, used for "edit on GitHub" / source links.
export const gitConfig = {
    user: 'rejifald',
    repo: 'bimorph',
    branch: 'main',
};
